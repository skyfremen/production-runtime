name: Dry Run Probe

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/dry-run-probe.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Fresh workflow registration PASS"
