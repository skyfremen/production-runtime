name: Run Caption Active Orange

on: [push]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: fix/punchline-active-orange-20260912
          fetch-depth: 0
      - name: Apply patch
        run: |
          python .github/scripts/apply_caption_active_orange.py
          python -m py_compile runtime/transform/process.py tests/test_align.py
          git diff --check
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add runtime/transform/process.py tests/test_align.py
          git commit -m "Use orange only for active punchline words"
          git push origin HEAD:fix/punchline-active-orange-20260912
