name: Base

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/base.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  base:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Probe
        run: echo "Base read-only probe PASS"
