name: Dry Run

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/dry-run.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dry-run:
    runs-on: ubuntu-latest
    steps:
      - name: Startup probe
        run: echo "Dry Run startup probe PASS"
