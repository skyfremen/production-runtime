name: Observe

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/observe.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  observe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Probe
        run: echo "Observe probe PASS"
